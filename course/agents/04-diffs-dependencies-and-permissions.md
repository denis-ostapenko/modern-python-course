# Inspect the whole proposal

Optional agent lab · 20 to 30 minutes

## Try

Review the dependency and migration suggestions in exercises/3.4/proposal.md. List every proposed changed surface.

## Build

Classify each change as required by the issue, optional but separately scoped, or outside the request. Use git diff --stat followed by git diff on your accepted change.

## Verify

The retained patch contains only the normalization behavior, adds no dependency, and leaves stored data unchanged.

Save your observations in an evidence note. If you revealed an answer, repeat with a new input before marking the practice complete. No live provider is required.
